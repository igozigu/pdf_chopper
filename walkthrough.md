# PDF 북마크 기반 자동 분할 프로그램 개발 Walkthrough

이 문서는 AI 코딩 에이전트(예: Claude Code, Cursor, GitHub Copilot Agent 등)에게 그대로 전달하여
작업을 지시할 수 있는 **완성된 프롬프트**입니다. 아래 "AI 에이전트에게 전달할 프롬프트" 섹션 전체를
복사해서 사용하세요.

---

## 1. 프로젝트 개요

- **목적**: PDF 파일을 업로드하면, 해당 PDF에 포함된 **북마크(목차) 항목 기준**으로 파일을 여러 개의
  PDF로 자동 분할하는 Windows용 데스크톱 프로그램 개발
- **배포 형태**: 폴더 최상단에 위치하는 **단일 실행파일(.exe)**
- **UI 방식**: 실행 시 팝업(GUI) 창이 뜨고, PDF 파일을 **드래그앤드롭**으로 업로드
- **출력 방식**: 원본 PDF가 있는 폴더 안에 **PDF 파일명과 동일한 이름의 하위 폴더**를 생성하고,
  분할된 PDF들을 그 안에 저장

---

## 2. 핵심 기능 요구사항

1. exe 실행 → 팝업 창(GUI) 표시
2. 팝업 창에 PDF 파일을 **드래그앤드롭**으로 업로드할 수 있는 영역 제공
   (파일 선택 버튼으로도 업로드 가능하게 하면 더 좋음, 필수는 아님)
3. 업로드된 PDF의 파일명, 경로, 페이지 수, 북마크 개수를 화면에 표시
4. **"작업 시작"** 버튼과 **"취소"** 버튼 제공
   - 작업 시작: 분할 작업 실행
   - 취소: 업로드된 파일 초기화 또는 프로그램 종료(작업 미실행)
5. PDF 내 **북마크(outline/bookmark) 계층**을 읽어서, 각 최상위(또는 지정 레벨) 북마크 항목이
   가리키는 페이지 범위를 기준으로 PDF를 분할
6. 분할된 각 PDF는 원본 파일이 위치한 폴더 안에 **원본 파일명과 동일한 이름의 새 폴더**를 만들어 저장
   - 예: `계약서.pdf` → `계약서/` 폴더 생성 → `01_서론.pdf`, `02_본론.pdf` … 저장
7. 분할된 각 PDF 파일명은 **북마크 제목 기반**으로 생성하되, 파일명에 사용할 수 없는 특수문자
   (`\ / : * ? " < > |`)는 제거 또는 치환 처리
8. 작업 완료 후 팝업으로 완료 메시지 표시(성공 개수, 저장 경로 안내)
9. 오류 발생 시(북마크 없음, 파일 손상, 암호화된 PDF 등) 사용자에게 명확한 에러 메시지 표시

---

## 3. 예외 처리 요구사항

- 북마크가 전혀 없는 PDF를 업로드한 경우 → "북마크가 없어 분할할 수 없습니다" 안내
- 암호화되어 있거나 열 수 없는 PDF → "PDF를 열 수 없습니다(암호 보호 등)" 안내
- 같은 이름의 출력 폴더가 이미 존재하는 경우 → 덮어쓸지 여부를 확인하거나 자동으로 번호를 붙여
  새 폴더 생성(`계약서(1)/`)
- 북마크 계층이 여러 단계(1단계, 2단계 등)로 되어 있는 경우 → 기본적으로 최상위(1단계) 북마크
  기준으로 분할하되, 필요 시 분할 기준 레벨을 선택할 수 있는 옵션 제공(선택 사항)
- 중첩 북마크의 마지막 항목은 해당 항목 시작 페이지부터 PDF의 끝 페이지까지로 처리

---

## 4. 기술 스택 권장 사항 (에이전트가 참고할 가이드, 강제 아님)

- **언어**: Python
- **PDF 처리 라이브러리**: `PyMuPDF (fitz)` 또는 `pypdf` (북마크/아웃라인 추출 및 페이지 분할 지원)
- **GUI 및 드래그앤드롭**: `tkinter` + `tkinterdnd2` (경량, exe 패키징 용이) 또는 `PySide6`/`PyQt`
  (더 세련된 UI가 필요할 경우)
- **exe 패키징**: `PyInstaller` (`--onefile` 옵션으로 단일 exe 생성)
- 위 스택이 어렵거나 제약이 있다면 에이전트가 판단하여 동등한 대안 기술 스택을 제안하고 사용 가능

---

## 5. 결과물 형태

- `dist/` 또는 프로젝트 최상단에 실행 가능한 **하나의 .exe 파일** 생성
- 소스 코드 전체(빌드 스크립트 포함)
- 간단한 사용법이 담긴 `README.md`
- 로컬에서 빌드 재현이 가능하도록 `requirements.txt` 및 빌드 명령어 포함

---

## AI 에이전트에게 전달할 프롬프트

아래 내용을 그대로 AI 코딩 에이전트에게 붙여넣어 작업을 지시하세요.

```
[역할]
너는 Python 기반 Windows 데스크톱 프로그램을 개발하는 시니어 개발자다.

[개발할 프로그램]
PDF 파일을 업로드하면, PDF에 포함된 북마크(목차) 항목을 기준으로 파일을 여러 개의 PDF로
자동 분할해주는 Windows용 프로그램을 만들어줘.

[배포 형태]
- 최종 결과물은 폴더 최상단 위치에 두고 바로 실행할 수 있는 단일 실행파일(.exe) 하나여야 한다.
- 별도 설치 과정 없이 더블클릭만으로 실행되어야 한다.
- PyInstaller의 --onefile 옵션 등을 사용해서 하나의 exe로 패키징해줘.

[UI 요구사항]
- exe를 실행하면 팝업(GUI) 창이 뜬다.
- 팝업 창에는 PDF 파일을 드래그앤드롭으로 업로드할 수 있는 영역이 있어야 한다.
  (파일 탐색기로 직접 선택하는 버튼도 함께 있으면 좋다.)
- 파일을 업로드하면 파일명, 페이지 수, 감지된 북마크 개수를 화면에 표시해줘.
- 팝업 창에는 "작업 시작" 버튼과 "취소" 버튼이 있어야 한다.
  - 작업 시작 버튼을 누르면 분할 작업이 실행된다.
  - 취소 버튼을 누르면 업로드된 파일을 초기화하거나 프로그램을 종료한다(분할 작업은 실행하지 않는다).
- 작업이 완료되면 몇 개의 파일로 분할되었는지, 저장 경로가 어디인지 팝업으로 안내해줘.

[핵심 로직]
- 업로드된 PDF의 북마크(아웃라인) 정보를 읽어서, 최상위 레벨 북마크 항목을 기준으로 페이지 범위를
  나누고 각 범위를 별도의 PDF 파일로 저장한다.
- 마지막 북마크 항목은 해당 시작 페이지부터 PDF의 마지막 페이지까지로 처리한다.
- 분할된 PDF 파일들은, 업로드된 원본 PDF 파일이 위치한 폴더 안에 "원본 PDF 파일명과 동일한 이름의
  새 폴더"를 만들어서 그 안에 저장한다.
  예) C:\문서\계약서.pdf 를 업로드하면 → C:\문서\계약서\ 폴더를 생성하고 그 안에 분할된 파일들을 저장한다.
- 분할된 각 PDF의 파일명은 해당 북마크의 제목을 기반으로 생성하되, 파일명에 사용할 수 없는 특수문자
  (\ / : * ? " < > |)는 제거하거나 밑줄(_)로 치환한다.
- 파일명 앞에 순번(01_, 02_ 등)을 붙여서 원래 순서를 알 수 있게 해줘.

[예외 처리]
- 북마크가 전혀 없는 PDF를 업로드한 경우: "이 PDF에는 북마크가 없어 분할할 수 없습니다"라는 안내
  메시지를 보여주고 작업을 진행하지 않는다.
- 암호로 보호되어 있거나 손상되어 열 수 없는 PDF인 경우: 명확한 에러 메시지를 보여준다.
- 출력 폴더(원본 파일명과 동일한 폴더)가 이미 존재하는 경우: 사용자에게 덮어쓸지 확인하거나,
  자동으로 "계약서(1)" 형태로 새 폴더를 만들어 저장한다.
- 북마크가 여러 계층(하위 북마크 포함)으로 되어 있는 경우, 기본적으로 최상위 레벨 기준으로만
  분할하도록 만들어줘.

[기술 스택 제안]
- 언어: Python
- PDF 처리: PyMuPDF(fitz) 또는 pypdf 중 북마크 추출과 페이지 분할이 더 안정적인 쪽을 선택
- GUI 및 드래그앤드롭: tkinter + tkinterdnd2 (또는 더 나은 대안이 있다면 제안하고 이유를 설명)
- exe 패키징: PyInstaller --onefile

[결과물]
1. 전체 소스 코드 (GUI 코드, PDF 분할 로직, 빌드 스크립트 포함)
2. requirements.txt
3. PyInstaller 빌드 명령어 또는 빌드 스크립트(.bat 파일 등)
4. 사용 방법을 설명하는 README.md
5. 코드에 한글 주석으로 각 함수의 역할을 간단히 설명

[진행 방식]
- 먼저 전체 아키텍처(파일 구조, 사용할 라이브러리, 함수 구성)를 간단히 설명하고,
- 그 다음 GUI 코드와 PDF 분할 로직 코드를 순서대로 작성해줘.
- 마지막으로 exe 빌드 방법과 테스트 방법을 안내해줘.
```

---

## 6. 프롬프트 사용 팁

- 에이전트가 코드를 생성한 뒤에는 실제 샘플 PDF(북마크 포함)로 테스트해보고, 파일명에 한글이나
  특수문자가 섞여도 정상 동작하는지 확인하는 것이 중요합니다.
- PyInstaller로 exe를 만들면 Windows Defender 등 백신 프로그램이 오탐지할 수 있으니, 배포 전
  실제 환경에서 실행 테스트를 권장합니다.
- 북마크 분할 기준(최상위 레벨만 vs 특정 레벨 지정)을 나중에 옵션으로 확장하고 싶다면, 위 프롬프트의
  "핵심 로직" 부분에 레벨 선택 UI 요구사항을 추가해서 다시 요청하면 됩니다.
